<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="br.com.compremelhor">

    <uses-permission android:name="android.permission.INTERNET" />

    <application android:allowBackup="true"
        android:label="@string/app_name"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar" >

        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/app_id" >
        </meta-data>

        <activity
            android:name=".controller.activity.LoginActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".controller.activity.ShoppingActivity"
            android:theme="@style/Theme.Example"
            android:label="@string/purchase_label">
            <meta-data
                android:name="android.support.UI_OPTIONS"
                android:value="splitActionBarWhenNarrow" />
        </activity>

        <activity
            android:name=".controller.activity.DashboardActivity"
            android:theme="@android:style/Theme.NoTitleBar" />

        <activity
            android:name=".controller.activity.ProfileActivity"
            android:label="@string/action_bar_profile_label" />
        <activity
            android:name=".controller.activity.PasswordActivity"
            android:label="@string/action_bar_change_password_label" />
        <activity android:name=".controller.activity.RegisterActivity" />
        <activity
            android:name=".controller.activity.AddressListActivity"
            android:label="@string/title_my_addresses" />
        <activity android:name=".controller.activity.AddressActivity" />

        <activity android:name="com.facebook.FacebookActivity" />

    </application>

</manifest>
